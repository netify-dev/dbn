% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forecast_gain.R
\name{forecast_gain_truth}
\alias{forecast_gain_truth}
\title{Truth forecast gain under a known data-generating process}
\usage{
forecast_gain_truth(
  Y,
  A_true,
  Theta_true,
  M_true,
  window = NULL,
  relation = 1L
)
}
\arguments{
\item{Y}{The observed data array (n x n x p x T).}

\item{A_true}{(n x n x T) truth operator path.}

\item{Theta_true}{(n x n x T) truth latent state path.}

\item{M_true}{(n x n) truth baseline.}

\item{window}{Integer vector of time points to sum over.}

\item{relation}{Index of the relation (default 1).}
}
\value{
Numeric vector of length n giving per-actor truth gain.
}
\description{
Companion to \code{forecast_gain()} for simulation studies. Computes
the per-actor gain using the true A_arr, Theta_arr, M_mat at each
time. Useful for calibration checks: posterior \code{forecast_gain()}
should be unbiased and well-calibrated for this target.
}
\examples{
\donttest{
sim <- simulate_dynamic_dbn(n = 6, p = 1, time = 8, seed = 1)
n <- dim(sim$A)[1]; Tt <- dim(sim$A)[3]
M_true <- matrix(0, n, n)  # simulator centers Theta at zero
Theta_true_3d <- array(sim$Theta[, , 1, ], c(n, n, Tt))
truth_gain <- dbn:::forecast_gain_truth(sim$Y, sim$A,
  Theta_true_3d, M_true,
  window = 5:8)
truth_gain
}
}
\keyword{internal}
