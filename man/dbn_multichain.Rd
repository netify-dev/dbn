% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multichain.R
\name{dbn_multichain}
\alias{dbn_multichain}
\title{Run multiple independent dbn chains}
\usage{
dbn_multichain(Y, chains = 2L, seeds = NULL, ..., parallel = TRUE)
}
\arguments{
\item{Y}{A 4D array \verb{[n_row, n_col, p, Tt]} (passed to \code{\link[=dbn]{dbn()}}).}

\item{chains}{Integer >= 1. Number of independent samplers to run.}

\item{seeds}{Integer vector of length \code{chains}, or \code{NULL} (default)
to choose deterministic seeds derived from \code{set.seed(NULL)}.}

\item{...}{All other arguments passed through to \code{\link[=dbn]{dbn()}} (e.g.
\code{model}, \code{family}, \code{nscan}, \code{burn}, \code{verbose}).}

\item{parallel}{Logical. If \code{TRUE} (default) and \code{future.apply} is
installed and a multisession plan is active, chains run in
parallel; otherwise sequentially.}
}
\value{
A list of class \code{dbn_multichain} with elements:
\itemize{
\item \code{chains}: list of K \code{dbn} fit objects.
\item \code{seeds}: integer vector of seeds actually used.
\item \code{n_chains}: K.
\item \code{args}: the captured \code{...} for reproducibility.
}
}
\description{
Wrapper that calls \code{\link[=dbn]{dbn()}} K times with K different seeds and bundles
the results into a \code{dbn_multichain} object. Each fit retains its full
draws; convergence summaries (Rhat, ESS) can then be computed across
chains via \code{\link[=check_convergence]{check_convergence()}} or via the standard \code{posterior}
package after \code{\link[=as_draws.dbn_multichain]{as_draws.dbn_multichain()}}.
}
\details{
Parallelism follows the \code{future} package's plan if installed and a
multisession plan is active; otherwise chains run sequentially. The
default \code{seeds = NULL} chooses \code{K} values deterministically from
\code{1:.Machine$integer.max} based on the system seed at call time.
}
\examples{
\donttest{
sim <- simulate_dynamic_dbn(n = 5, time = 6, seed = 1)
mc <- dbn_multichain(sim$Z, chains = 2, seeds = c(1, 2),
                     model = "dynamic", family = "gaussian",
                     nscan = 150, burn = 50, verbose = FALSE)
length(mc$chains)
}
}
\seealso{
\code{\link[=dbn]{dbn()}}, \code{\link[=check_convergence]{check_convergence()}}, \code{\link[=as_draws.dbn_multichain]{as_draws.dbn_multichain()}}
}
\author{
Tosin Salau and Shahryar Minhas
}
