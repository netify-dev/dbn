% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extras.R
\name{coupling_trajectory}
\alias{coupling_trajectory}
\title{Coupling trajectory with credible bands}
\usage{
coupling_trajectory(fit, actors = NULL, level = 0.95)
}
\arguments{
\item{fit}{A dynamic / piecewise / lowrank / hmm fit with operator
draws stored under \code{fit$A}, \code{fit$B}.}

\item{actors}{Optional integer vector of actor indices, or character
vector of actor names. Default: all actors.}

\item{level}{Credible level (default 0.95).}
}
\value{
A data frame with columns \code{actor}, \code{t}, \code{mean}, \code{lo}, \code{hi}.
}
\description{
Returns a tidy data frame of per-actor coupling
\verb{C_i(t) = ||W_t[i, ]||_2} over time with posterior credible
intervals. Unlike \code{\link[=actor_embedding]{actor_embedding()}}, which reports a single
per-(actor, time) point estimate, this helper exposes the per-draw
trajectory so you can plot ribbons.
}
\examples{
\donttest{
sim <- simulate_dynamic_dbn(n = 6, time = 10, seed = 1)
fit <- dbn(sim$Z, model = "dynamic", family = "gaussian",
           nscan = 200, burn = 100, verbose = FALSE)
ct <- coupling_trajectory(fit)
head(ct)
}
}
\author{
Tosin Salau and Shahryar Minhas
}
