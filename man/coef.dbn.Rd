% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dbn_accessors.R
\name{coef.dbn}
\alias{coef.dbn}
\title{Extract estimated operators and baseline mean from a dbn fit}
\usage{
\method{coef}{dbn}(object, what = c("all", "A", "B", "M"), ...)
}
\arguments{
\item{object}{A \code{dbn} fit object.}

\item{what}{Character. Which quantity to extract. One of \code{"all"}
(default; named list with A, B, M), \code{"A"}, \code{"B"}, \code{"M"}.}

\item{...}{Unused.}
}
\value{
If \code{what = "all"}, a named list with elements \code{A}, \code{B}, \code{M}.
Otherwise a single array. Operator arrays are
\verb{[n_row, n_row, T]} (A) and \verb{[n_col, n_col, T]} (B); the baseline \code{M}
is \verb{[n_row, n_col, p]}.
}
\description{
For MCMC fits, returns posterior MEANS of the operator trajectories.
For ALS fits (static or TV), returns the point estimate.
}
\details{
Static MCMC fits have no transition operator; their \code{A} slot returns the
Tucker sender factor (the n_row x n_row matrix \code{fit$B[[1]]} averaged across
draws) and \code{B} returns the Tucker receiver factor.
}
\author{
Tosin Salau and Shahryar Minhas
}
