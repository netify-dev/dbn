% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/forecast_gain.R
\name{forecast_gain}
\alias{forecast_gain}
\title{Actor-level forecast gain from a fitted dynamic bilinear model}
\usage{
forecast_gain(fit, Y, window = NULL, relation = 1L)
}
\arguments{
\item{fit}{A \code{dbn_dynamic} fit (or compatible posterior list). Must
contain \verb{$Theta} (n x n x p x T x M), \verb{$A} (list of length M of
n x n x T arrays), \verb{$M} (n x n x p x M), \verb{$sigma2}, \verb{$sigma2_obs}.}

\item{Y}{The observed data array (n x n x p x T). Required because
the fit object may store only kept time slices.}

\item{window}{Integer vector of time points to sum over. Defaults to
all t >= 2 (forecast gain is undefined at t = 1).}

\item{relation}{Index of the relation (p dimension) to use. Default 1.}
}
\value{
A list with \code{per_actor} (n x M matrix of per-draw gains,
summed over \code{window}), \code{summary} (a data frame with mean, sd,
q025, q500, q975 per actor), and \code{attrs} (window, relation, n
for reference).
}
\description{
Computes per-actor predictive gain by comparing the dynamic model's
one-step-ahead predictions for each dyad against a static-baseline
prediction. For Gaussian-family fits the gain is MSE-based; for
ordinal / rank likelihood it uses log score.
}
\details{
For each posterior draw m and each time t in window E:
\itemize{
\item predicted Y under the dynamic model is M + A_t Theta_{t-1} A_t'
\item predicted Y under the baseline is M alone
Per-dyad gain at (i, j, t):
G_{ij,t}^{(m)} = (Y_{ij,t} - M_{ij}^{(m)})^2
- (Y_{ij,t} - M_{ij}^{(m)} - (A_t^{(m)} Theta_{t-1}^{(m)} A_t^{(m)'})\emph{{ij})^2
Per-actor gain (Gaussian, MSE-form):
G_i^{(m)}(E) = sum}{t in E} sum_{j != i} G_{ij,t}^{(m)}
}

Returns one posterior summary per actor plus the per-draw matrix
so the caller can compute multi-chain R-hat / coverage.
}
\examples{
\donttest{
sim <- simulate_dynamic_dbn(n = 6, p = 1, time = 10, seed = 1)
fit <- dbn_dynamic(sim$Y, family = "gaussian",
                   nscan = 200, burn = 100, time_thin = 1L,
                   verbose = FALSE)
fg <- forecast_gain(fit, sim$Y, window = 8:10, relation = 1)
head(fg$summary)
}
}
