% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dbn_accessors.R
\name{node_embedding}
\alias{node_embedding}
\title{Latent node positions from a low-rank dbn fit}
\usage{
node_embedding(fit, align = TRUE)
}
\arguments{
\item{fit}{A \code{dbn} fit with \code{model = "lowrank"}. Other model variants
abort with a directive message.}

\item{align}{Logical; if \code{TRUE} (default), align column signs across
draws before averaging. Set to \code{FALSE} for the raw posterior mean
(which will likely cancel toward zero column-by-column).}
}
\value{
An \verb{n_row x r} matrix with \code{rownames} set from
\code{dimnames(fit$Y)[[1]]} when available.
}
\description{
Returns the posterior-mean \verb{n_row x r} node-position
matrix from a \code{model = "lowrank"} fit, with column signs aligned
across draws via the Procrustes-style trick of fixing the first
draw and flipping subsequent draws' column signs to maximise their
inner product with the reference. Unlike \code{\link[=actor_embedding]{actor_embedding()}},
which returns a scalar (the row-norm) per actor per time, this
accessor returns the full latent position so the geometry is
inspectable.
}
\examples{
\donttest{
sim <- simulate_lowrank_dbn(n = 8, p = 1, time = 10, r = 2, seed = 1)
fit <- dbn(sim$Z, model = "lowrank", family = "gaussian", r = 2,
           nscan = 200, burn = 100, verbose = FALSE)
U <- node_embedding(fit)
dim(U)  # n_row x r
}
}
\author{
Tosin Salau and Shahryar Minhas
}
