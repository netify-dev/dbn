% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coercion.R
\name{dbn_coercion}
\alias{dbn_coercion}
\alias{as_dbn_array.network}
\alias{as_dbn_array.list}
\alias{as_dbn_array.igraph_panel}
\alias{as_dbn_array.networkDynamic}
\alias{as_dbn_array_edgelist}
\title{Coerce common network formats to the dbn 4D array}
\usage{
\method{as_dbn_array}{network}(x, ...)

\method{as_dbn_array}{list}(x, ...)

\method{as_dbn_array}{igraph_panel}(x, attr = NULL, time_labels = NULL, ...)

\method{as_dbn_array}{networkDynamic}(x, onsets = NULL, ...)

as_dbn_array_edgelist(
  x,
  actors = NULL,
  time_levels = NULL,
  weight = NULL,
  symmetric = FALSE
)
}
\arguments{
\item{x}{A tidy edgelist data frame with columns
\code{from}, \code{to}, \code{time}, and optionally \code{weight}.}

\item{...}{Method-specific arguments forwarded to the dispatched
coercion method.}

\item{attr}{Edge attribute to extract (numeric weight). If \code{NULL} and
the graph is unweighted, the binary adjacency is used.}

\item{time_labels}{Optional character vector of time labels (length
\code{T}). Defaults to \code{seq_along(x)}.}

\item{onsets}{Numeric vector of time points to snapshot at. Default
uses \code{networkDynamic::get.change.times(x)}.}

\item{actors}{Optional character vector of canonical actor labels in
the order they should appear in the array. Defaults to
\code{sort(unique(c(x$from, x$to)))}.}

\item{time_levels}{Optional vector of time-axis levels. Defaults to
\code{sort(unique(x$time))}.}

\item{weight}{Column name (string) holding the weight. If \code{NULL},
uses 1 for every observed edge (binary).}

\item{symmetric}{Logical; if \code{TRUE}, mirror across the diagonal.}
}
\value{
A 4D numeric array of shape \verb{[n_row, n_col, 1, T]} with
\code{dimnames} populated from the source object when available, suitable
for direct use as the first argument to \code{dbn()}.
}
\description{
\code{dbn()} and friends consume a 4D \verb{[n_row, n_col, p, T]} array (with
diagonal \code{NA} on unipartite slices). Most R users start with an
\code{igraph} panel, a \code{statnet} \code{networkDynamic}, or a tidy edgelist.
These helpers do the coercion so callers don't have to write the
\code{array(NA_real_, dim = ...)} skeleton themselves.
}
\details{
All coercions assume a single relation (\code{p = 1}) by default. For
multi-relation data, call the helper once per relation and stack with
\code{abind::abind} along the \code{p} axis. Diagonal entries on unipartite
(square) outputs are set to \code{NA} to match the dbn convention.
}
\author{
Tosin Salau and Shahryar Minhas
}
