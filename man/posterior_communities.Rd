% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extras.R
\name{posterior_communities}
\alias{posterior_communities}
\title{Posterior communities from a low-rank dbn fit}
\usage{
posterior_communities(fit, k = NULL, ...)
}
\arguments{
\item{fit}{A \code{model = "lowrank"} fit.}

\item{k}{Integer number of communities. Defaults to \code{ceiling(sqrt(n))}.}

\item{...}{Forwarded to \code{\link[stats:kmeans]{stats::kmeans()}}.}
}
\value{
A list with \code{membership} (named integer vector of community
labels), \code{centers} (k x r matrix of cluster centroids), and \code{U}
(the n x r embedding clustered).
}
\description{
Clusters actors by their latent-space positions
\code{node_embedding(fit)} into \code{k} communities using k-means. The
columns of \code{U} are sign-aligned across draws before clustering so
the result is invariant to the per-draw sign indeterminacy.

Closer in spirit to a low-rank spectral embedding than to a
stochastic block model: actors in the same community share
similar influence-row patterns rather than identical edge
probabilities. Use \code{dynsbm} or \code{blockmodels} for SBM-proper
inference; this helper is for descriptive partitioning.
}
\examples{
\donttest{
sim <- simulate_lowrank_dbn(n = 10, p = 1, time = 8, r = 2, seed = 1)
fit <- dbn(sim$Z, model = "lowrank", family = "gaussian", r = 2,
           nscan = 200, burn = 100, verbose = FALSE)
pc <- posterior_communities(fit, k = 3)
pc$membership
}
}
\author{
Tosin Salau and Shahryar Minhas
}
