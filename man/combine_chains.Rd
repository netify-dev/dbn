% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multichain.R
\name{combine_chains}
\alias{combine_chains}
\title{Combine the chains of a \code{dbn_multichain} into a single \code{dbn} fit}
\usage{
combine_chains(mc)
}
\arguments{
\item{mc}{A \code{dbn_multichain} object.}
}
\value{
A single \code{dbn} fit object with all draws concatenated.
}
\description{
Concatenates per-draw operator lists (\code{A}, \code{B}, \code{M}) and scalar
parameter vectors (\code{sigma2}, \code{tau_A2}, \code{tau_B2}) across chains. The
result is a single \code{dbn} object with all \code{K * n_draws} draws stacked,
suitable for every standard accessor (\code{tidy}, \code{coef}, \code{predict},
\code{gof}, \code{compute_irf}, ...).
}
\details{
This is the brms-style "merged-chains" view. Use it when you want a
single posterior summary that respects all chains; use the underlying
\code{mc$chains} when you need per-chain access.
}
\examples{
\donttest{
sim <- simulate_dynamic_dbn(n = 5, time = 6, seed = 1)
mc <- dbn_multichain(sim$Z, chains = 2, seeds = c(1, 2),
                     model = "dynamic", family = "gaussian",
                     nscan = 150, burn = 50, verbose = FALSE)
fit_all <- combine_chains(mc)
length(fit_all$sigma2)  # 2 * nscan
}
}
\seealso{
\code{\link[=dbn_multichain]{dbn_multichain()}}, \code{\link[=check_convergence]{check_convergence()}} (multichain).
}
\author{
Tosin Salau and Shahryar Minhas
}
