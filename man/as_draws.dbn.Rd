% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multichain.R
\name{as_draws.dbn}
\alias{as_draws.dbn}
\title{Convert dbn fit to a posterior::draws_array}
\usage{
as_draws.dbn(x, include_operators = FALSE, ...)
}
\arguments{
\item{x}{A \code{dbn} fit.}

\item{include_operators}{Logical (default \code{FALSE}). If \code{TRUE}, expand
per-cell entries (variable names of the form \code{A[i,j,t]},
\code{B[i,j,t]}, \code{M[i,j]}) as separate posterior variables.}

\item{...}{Unused.}
}
\value{
A \code{posterior::draws_array} (chains = 1).
}
\description{
Single-chain analog of \code{\link[=as_draws.dbn_multichain]{as_draws.dbn_multichain()}}. Useful for
calling \code{posterior::summarise_draws()} / \code{bayesplot} on a one-chain
fit. Multi-chain Rhat is meaningless on a single chain but
rank-normalised ESS still works.
}
\examples{
\donttest{
sim <- simulate_dynamic_dbn(n = 5, time = 6, seed = 1)
fit <- dbn(sim$Z, model = "dynamic", family = "gaussian",
           nscan = 200, burn = 100, verbose = FALSE)
if (requireNamespace("posterior", quietly = TRUE)) {
  da <- as_draws.dbn(fit)
  dim(da)
}
}
}
\author{
Tosin Salau and Shahryar Minhas
}
