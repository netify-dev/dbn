% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/models_hmm.R
\name{dbn_hmm}
\alias{dbn_hmm}
\title{DBN HMM Model}
\usage{
dbn_hmm(
  Y,
  family = c("ordinal", "gaussian", "binary"),
  R = 3,
  nscan = 10000,
  burn = 1000,
  odens = 1,
  delta = rep(1, R),
  seed = 6886,
  verbose = TRUE,
  time_thin = 1,
  previous = NULL,
  init = NULL,
  symmetric = FALSE,
  ...
)
}
\arguments{
\item{Y}{Data array (nodes x nodes x relations x time)}

\item{family}{Data family (ordinal, gaussian, or binary)}

\item{R}{Number of regimes}

\item{nscan}{Number of iterations of the Markov chain (beyond burn-in)}

\item{burn}{Burn-in for the Markov chain}

\item{odens}{Output density for the Markov chain}

\item{delta}{Dirichlet prior for transition matrix}

\item{seed}{Random seed}

\item{verbose}{Print progress}

\item{time_thin}{Save every nth time point, when 1, save all time points}

\item{previous}{Previous dbn_hmm results to continue from (optional)}

\item{init}{List of initial values: S, A_list, B_list, Pi, sigma2_proc, tau_A2, tau_B2, g2, pi0 (optional)}

\item{symmetric}{Logical. If TRUE, enforce B = A for each regime. Default: FALSE.}

\item{...}{Additional arguments}
}
\value{
List containing MCMC results
}
\description{
Regime-switching DBN for large-scale networks (100+ actors, 50+ time steps)
}
\examples{
\donttest{
sim <- simulate_hmm_dbn(n = 8, time = 10, R = 2, seed = 1)
fit <- dbn_hmm(sim$Y, R = 2, nscan = 200, burn = 100, verbose = FALSE)
}
}
\seealso{
\code{\link{dbn}} for the main dispatcher, \code{\link{param_summary}} for posterior summaries
}
