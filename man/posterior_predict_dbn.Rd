% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posterior_predict.R
\name{posterior_predict_dbn}
\alias{posterior_predict_dbn}
\title{Generate Posterior Predictive Samples}
\usage{
posterior_predict_dbn(
  fit,
  draws = 100,
  seed = NULL,
  draw_indices = NULL,
  simplify = FALSE,
  ...
)
}
\arguments{
\item{fit}{A dbn model fit object (output from \code{\link[=dbn]{dbn()}})}

\item{draws}{Number of replicated datasets to generate (default: 100)}

\item{seed}{Random seed for reproducibility}

\item{draw_indices}{Optional integer vector of specific posterior draw
indices to use. When supplied, overrides \code{draws} (number replicated is
set to \code{length(draw_indices)}).}

\item{simplify}{Logical. If \code{TRUE}, collapse the list of replicated
arrays into a single array with the draw axis appended. Default
\code{FALSE}.}

\item{...}{Reserved for forward compatibility (e.g. \code{ndraws} from
\code{brms}-style callers).}
}
\value{
A list of replicated data arrays, each with the same dimensions
as the original data. Has class \code{"dbn_ppd"}.
}
\description{
Simulates new datasets from the fitted model. For each
posterior draw of the model parameters, generates a complete replicated
dataset. These replications can be compared to the observed data using
\code{\link[=plot_ppc_ecdf]{plot_ppc_ecdf()}} or \code{\link[=plot_ppc_density]{plot_ppc_density()}} to check whether the model
captures the key features of the data (a "posterior predictive check").
}
\examples{
\donttest{
sim <- simulate_dynamic_dbn(n = 6, time = 5, seed = 1)
fit <- dbn(sim$Y, model = "dynamic", nscan = 200, burn = 100, verbose = FALSE)
ppd <- posterior_predict_dbn(fit, draws = 5)
}
}
\seealso{
\code{\link{plot_ppc_ecdf}}, \code{\link{plot_ppc_density}}, \code{\link{param_summary}}
}
\author{
Tosin Salau and Shahryar Minhas
}
