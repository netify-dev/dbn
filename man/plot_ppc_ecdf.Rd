% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posterior_predict.R
\name{plot_ppc_ecdf}
\alias{plot_ppc_ecdf}
\title{Posterior predictive ECDF plot}
\usage{
plot_ppc_ecdf(
  fit,
  ppd = NULL,
  n_show = 20,
  alpha = 0.3,
  rel = 1,
  time = NULL,
  Y_obs = NULL
)
}
\arguments{
\item{fit}{A dbn model fit object}

\item{ppd}{Posterior predictive samples (from \code{posterior_predict_dbn})}

\item{n_show}{Number of replicates to display (default: 20)}

\item{alpha}{Transparency for replicate lines (default: 0.3)}

\item{rel}{Relation index to plot (default: 1)}

\item{time}{Time index to plot (default: all)}

\item{Y_obs}{Observed data array (required)}
}
\value{
A ggplot object
}
\description{
Plot empirical CDFs of observed vs replicated data
}
\examples{
\donttest{
sim <- simulate_static_dbn(n = 6, time = 5, seed = 1)
fit <- dbn(sim$Y, model = "static", nscan = 200, burn = 100, verbose = FALSE)
ppd <- posterior_predict_dbn(fit, draws = 5)
if (requireNamespace("ggplot2", quietly = TRUE)) plot_ppc_ecdf(fit, ppd, Y_obs = sim$Y)
}
}
\seealso{
\code{\link{posterior_predict_dbn}}, \code{\link{plot_ppc_density}}, \code{\link{param_summary}}
}
\author{
Tosin Salau and Shahryar Minhas
}
