% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/irf.R
\name{build_shock}
\alias{build_shock}
\title{Build Shock Matrix for IRF Analysis}
\usage{
build_shock(
  m,
  type = c("unit_edge", "node_out", "node_in", "density"),
  i = 1,
  j = 2,
  magnitude = 1,
  n_col = m
)
}
\arguments{
\item{m}{Number of actors (senders), or a fitted \code{dbn} object. When a
fitted object is passed, the network size and actor names are read off
the fit so \code{i} and \code{j} may be supplied as character strings.}

\item{type}{Type of shock:
\itemize{
\item \code{"unit_edge"}: perturb a single directed edge (i -> j)
\item \code{"node_out"}: perturb all outgoing edges from actor i
\item \code{"node_in"}: perturb all incoming edges to actor i
\item \code{"density"}: uniform perturbation to all edges
}}

\item{i}{Source actor (sender). Integer index or character name; the
latter is matched against \code{dimnames(fit$Y)[[1]]} when \code{m} is a fit.}

\item{j}{Target actor (receiver), used only for \code{"unit_edge"}. Integer
index or character name (matched against \code{dimnames(fit$Y)[[2]]}).}

\item{magnitude}{Size of the perturbation. Scale relative to your data:
e.g., if data ranges from -1 to 1, a magnitude of 0.5 is a large shock.}

\item{n_col}{Number of receiver nodes (default: same as \code{m}). Ignored
when \code{m} is a fitted \code{dbn} object.}
}
\value{
An \verb{m x n_col} matrix of zeros with the specified entries set to
\code{magnitude}. Pass this to \code{\link[=compute_irf]{compute_irf()}} as the \code{shock} argument.
}
\description{
Creates structured perturbation matrices for impulse response
analysis. The shock matrix S is added to the latent network at a single
time point, and \code{\link[=compute_irf]{compute_irf()}} tracks how the perturbation propagates.
}
\details{
\strong{When to use each shock type:}
\itemize{
\item \code{"unit_edge"}: Shock a single bilateral tie (e.g., "what if USA-Russia
relations improve?"). Use when you care about a specific dyad.
\item \code{"node_out"}: Shock all outgoing ties from one actor (e.g., "what if
China engages all partners at once?"). Use when you care about one
actor's overall engagement.
\item \code{"node_in"}: Shock all incoming ties to one actor.
\item \code{"density"}: Apply a uniform shock to all edges. Use to study how
a system-wide shift propagates.
}

For \strong{symmetric (undirected) networks}, symmetrize the shock after
building it: \code{S = S + t(S)}.
}
\examples{
# Integer indices (legacy interface)
S <- build_shock(m = 5, type = "unit_edge", i = 1, j = 2)

# Named actors via a fitted object
\dontrun{
  Y <- simulate_dynamic_dbn(n = 5, time = 8)$Z
  dimnames(Y) <- list(c("USA","UK","CHN","RUS","DEU"),
                      c("USA","UK","CHN","RUS","DEU"), "tie", NULL)
  fit <- dbn(Y, model = "dynamic", nscan = 200, burn = 100, verbose = FALSE)
  S <- build_shock(fit, type = "unit_edge", i = "USA", j = "CHN")
}

# Node-level shock: activate all outgoing edges from node 1
S_out <- build_shock(m = 5, type = "node_out", i = 1)

# Density shock: small shock spread across the network
S_dens <- build_shock(m = 5, type = "density", magnitude = 0.1)

# Bipartite shock: 4 senders, 6 receivers
S_bip <- build_shock(m = 4, n_col = 6, type = "unit_edge", i = 1, j = 3)
}
\seealso{
\code{\link{compute_irf}}, \code{\link{plot.dbn_irf}}
}
\author{
Tosin Salau and Shahryar Minhas
}
