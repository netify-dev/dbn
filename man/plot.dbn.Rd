% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dispatch.R
\name{plot.dbn}
\alias{plot.dbn}
\title{Plot Diagnostics for a Fitted DBN Model}
\usage{
\method{plot}{dbn}(x, ...)
}
\arguments{
\item{x}{A fitted \code{dbn} object returned by \code{\link[=dbn]{dbn()}}.}

\item{...}{Additional arguments passed to model-specific plot functions
(e.g., \code{alpha} for edge significance in the static model).}
}
\value{
A ggplot2 object (or arranged multi-panel plot) is printed and
returned invisibly.
}
\description{
Creates model-specific diagnostic plots for a fitted DBN
object. The type of plot depends on the model variant:
\itemize{
\item \strong{Static}: trace plots, posterior histograms, and a network
summary of the estimated B matrix.
\item \strong{Dynamic}: trace plots for variance parameters and, if
available, time-varying A/B summaries.
\item \strong{Lowrank}: trace plots, estimated factor trajectories
(\eqn{\alpha_t}), and the posterior mean node-loading matrix U.
\item \strong{HMM}: regime probabilities over time, the estimated
transition matrix, and MCMC trace plots.
\item \strong{Piecewise}: trace plots for each regime block.
}
}
\examples{
\donttest{
sim <- simulate_static_dbn(n = 8, time = 10, seed = 6886)
fit <- dbn(sim$Y, model = "static", nscan = 200, burn = 100, verbose = FALSE)
plot(fit)
}
}
\seealso{
\code{\link[=dbn]{dbn()}}, \code{\link[=plot_trace]{plot_trace()}}, \code{\link[=check_convergence]{check_convergence()}}, \code{\link[=summary.dbn]{summary.dbn()}}
}
