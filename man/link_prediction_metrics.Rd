% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extras.R
\name{link_prediction_metrics}
\alias{link_prediction_metrics}
\title{Held-out link-prediction metrics for a dbn fit}
\usage{
link_prediction_metrics(
  fit,
  Y_truth = NULL,
  held_out_mask = NULL,
  held_out_time = NULL,
  threshold_truth = 0,
  K = c(10L, 50L)
)
}
\arguments{
\item{fit}{A fitted \code{dbn} object (typically \code{family = "binary"} for
AUC/AP semantics; other families produce continuous predictions
and the metrics are computed against the binarized truth at
\code{threshold_truth}).}

\item{Y_truth}{Either a 4D \verb{[n_row, n_col, p, T]} array of truth
(same shape as \code{fit$Y}) or \code{NULL}. If \code{NULL}, \code{fit$Y} is used and
the user MUST provide \code{held_out_mask} or \code{held_out_time} so we
don't score against the training data.}

\item{held_out_mask}{Logical 4D array marking held-out cells (TRUE
= held out). Mutually exclusive with \code{held_out_time}.}

\item{held_out_time}{Integer vector of held-out time indices.}

\item{threshold_truth}{Numeric. For non-binary families, dichotomize
\code{Y_truth} at this threshold to define positive ties. Ignored for
\code{family = "binary"}.}

\item{K}{Integer or integer vector of K values for precision@K.
Default \code{c(10, 50)}.}
}
\value{
A list with components \code{auc}, \code{ap} (average precision),
\code{accuracy}, \code{precision_at_K} (named numeric vector), \code{n_pos},
\code{n_neg}, and \code{held_out} (the mask actually used). Returns NA
metrics with a directive when there are fewer than 2 positives.
}
\description{
Computes the standard binary-classification metrics
used in the graph-ML literature (AUC, average precision,
precision@K, accuracy at a given threshold) against held-out dyads
or a held-out time slice. Designed for users who want to compare
dbn to GNN baselines on the same evaluation protocol.
}
\author{
Tosin Salau and Shahryar Minhas
}
