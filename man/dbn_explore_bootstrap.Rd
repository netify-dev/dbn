% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dispatch.R
\name{dbn_explore_bootstrap}
\alias{dbn_explore_bootstrap}
\title{Bootstrap Inference for ALS Fits}
\usage{
dbn_explore_bootstrap(
  als_fit,
  R = 200,
  type = c("block", "parametric"),
  seed = NULL,
  verbose = TRUE
)
}
\arguments{
\item{als_fit}{A fitted dbn object from \code{\link[=dbn_explore]{dbn_explore()}}.}

\item{R}{Integer. Number of bootstrap replicates. Default is 200. Increase
to 500-1000 for publication-quality intervals.}

\item{type}{Character. Bootstrap type: \code{"block"} (default) resamples time
periods with replacement; \code{"parametric"} simulates new outcomes from the
fitted model.}

\item{seed}{Optional integer for reproducibility. Sets the random seed
before resampling.}

\item{verbose}{Logical. If TRUE, prints progress every 10 replicates and
warnings about convergence issues.}
}
\value{
An object of class \code{"dbn_boot"} with components:
\itemize{
\item \code{coefs_A}: R x (n_row * n_row) matrix of vectorized bootstrap A estimates
\item \code{coefs_B}: R x (n_col * n_col) matrix of vectorized bootstrap B estimates
\item \code{coefs_M}: R x (n_row * n_col * p) matrix of vectorized bootstrap M estimates
\item \code{se_A}, \code{se_B}, \code{se_M}: Standard errors (column sds of valid replicates)
\item \code{ci_A_lo}, \code{ci_A_hi}: 2.5\% and 97.5\% percentile bounds for A
\item \code{ci_B_lo}, \code{ci_B_hi}: Percentile bounds for B
\item \code{ci_M_lo}, \code{ci_M_hi}: Percentile bounds for M
\item \code{point_est_A}, \code{point_est_B}, \code{point_est_M}: Original ALS estimates
\item \code{n_valid}: Number of successful bootstrap replicates
\item \code{n_total}: Total number of replicates attempted
\item \code{type}: The bootstrap type used (\code{"block"} or \code{"parametric"})
\item \code{family}: The distribution family
\item \code{symmetric}: Whether the symmetric constraint was used
\item \code{dims}: Network dimensions (n_row, n_col, p, Tt)
}
}
\description{
Computes bootstrap standard errors and confidence intervals for ALS point
estimates from \code{\link[=dbn_explore]{dbn_explore()}}. Two bootstrap strategies are available:
\itemize{
\item \strong{Block bootstrap}: Resamples time periods (t indices) with replacement.
Preserves within-period dependence structure. Recommended when Tt >= 10.
\item \strong{Parametric bootstrap}: Simulates new outcome arrays from the fitted model
using estimated parameters and the specified family distribution. Better
when Tt is small but the model is well-specified.
}

Each replicate refits the full ALS model. Replicates that fail to converge
are dropped and reported. Standard errors are column standard deviations of
the successful replicates. Confidence intervals use the percentile method.
}
\examples{
\dontrun{
sim <- simulate_dynamic_dbn(n = 8, time = 10, seed = 1)
fit_als <- dbn_explore(sim$Y, family = "gaussian", verbose = FALSE)

# Block bootstrap with 200 replicates
boot_result <- dbn_explore_bootstrap(fit_als, R = 200, seed = 42)
print(boot_result)
summary(boot_result)

# Parametric bootstrap
boot_par <- dbn_explore_bootstrap(fit_als, R = 200, type = "parametric")
}

}
\seealso{
\code{\link[=dbn_explore]{dbn_explore()}}, \code{\link[=print.dbn_boot]{print.dbn_boot()}}
}
