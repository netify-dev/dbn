% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/models_lowrank.R
\name{dbn_lowrank}
\alias{dbn_lowrank}
\title{DBN Low-rank Model}
\usage{
dbn_lowrank(
  Y,
  family = c("ordinal", "gaussian", "binary"),
  r = 2,
  nscan = 10000,
  burn = 1000,
  odens = 1,
  ar1_alpha = TRUE,
  update_rho_alpha = FALSE,
  ar1_B = FALSE,
  update_rho_B = FALSE,
  seed = 6886,
  verbose = TRUE,
  time_thin = 1,
  previous = NULL,
  init = NULL,
  symmetric = FALSE,
  ...
)
}
\arguments{
\item{Y}{Data array (nodes x nodes x relations x time)}

\item{family}{Data family}

\item{r}{Rank for low-rank factorization. A good starting point is
\code{ceiling(log2(n))} where \code{n} is the number of nodes. Increase
if posterior predictive checks show poor fit. Default: 2.}

\item{nscan}{Number of iterations of the Markov chain (beyond burn-in)}

\item{burn}{Burn-in for the Markov chain}

\item{odens}{Output density for the Markov chain}

\item{ar1_alpha}{Use AR(1) for alpha dynamics}

\item{update_rho_alpha}{Update AR coefficient for alpha}

\item{ar1_B}{Use AR(1) for B dynamics}

\item{update_rho_B}{Update AR coefficient for B}

\item{seed}{Random seed}

\item{verbose}{Logical or numeric. TRUE prints every 100 iterations, numeric prints every n iterations, FALSE suppresses output.}

\item{time_thin}{Save every nth time point}

\item{previous}{Previous results to continue from}

\item{init}{Initial values}

\item{symmetric}{Logical. Not supported for low-rank models (will error). Default: FALSE.}

\item{...}{Additional arguments (currently unused)}
}
\value{
A \code{dbn} object with components including \code{U}, \code{alpha}, \code{B}, \code{M},
\code{Theta}, \code{sigma2}, \code{tau_A2}, \code{tau_B2}, \code{g2}, plus an \code{info} list of
model settings. See \code{summary()} and \code{plot()} methods for inspection,
and \code{tidy_dbn_lowrank()} for posterior summaries.
}
\description{
Fits a dynamic bilinear network model with a low-rank
factorization of the sender operator. Recommended for moderate-to-large
networks (n >= 25) where the full-rank \code{dbn_dynamic()} is memory- or
compute-limited.
}
\examples{
\donttest{
sim <- simulate_lowrank_dbn(n = 8, time = 5, r = 2, seed = 1)
fit <- dbn_lowrank(sim$Y, r = 2, nscan = 200, burn = 100, verbose = FALSE)
}
}
\seealso{
\code{\link[=dbn]{dbn()}} for the main dispatcher, \code{\link[=param_summary]{param_summary()}} for posterior
summaries, \code{\link[=tidy_dbn_lowrank]{tidy_dbn_lowrank()}} for low-rank-specific tidiers.
}
