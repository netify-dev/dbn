% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/irf.R
\name{dbn_leverage}
\alias{dbn_leverage}
\title{Actor-level leverage (impulse-response propagation magnitude)}
\usage{
dbn_leverage(fit, H = 3, t0 = 1, magnitude = 1, actors = NULL)
}
\arguments{
\item{fit}{A fitted dynamic \code{dbn} object. Static fits are rejected: with a
constant operator a node shock propagates identically for every actor, so
per-actor leverage is not identified.}

\item{H}{Propagation horizon (number of steps; default 3).}

\item{t0}{The time index at which the shock is applied (default 1). Must
satisfy \code{t0 + H <= T}.}

\item{magnitude}{Size of the unit out-shock (default 1).}

\item{actors}{Optional character labels for the actors.}
}
\value{
A data frame of class \code{dbn_leverage}, one row per actor, with the
posterior \code{mean}, \code{sd}, and 95\% credible interval (\code{q025}, \code{q975}) of
actor leverage. For a point-estimate (\code{\link[=dbn_als]{dbn_als()}}) fit the interval
collapses to the point estimate.
}
\description{
Leverage measures how widely a standardized shock involving an actor is
predicted to spread through the network over a finite horizon. For each
actor, \code{dbn_leverage()} applies a unit out-shock to that actor, propagates
it through the fitted lag operator with the package's impulse-response
machinery, and reports the average absolute network-wide effect over
horizons 1 to \code{H}.

Leverage is distinct from coupling. Coupling (the row magnitude of the lag
operator \eqn{W_t}, see \code{\link[=dbn_coupling_rank_probs]{dbn_coupling_rank_probs()}}) asks whether the
previous network helps predict an actor's next-period involvement. Leverage
asks how far a shock involving the actor propagates once it occurs. An actor
can be high coupling and low leverage, or the reverse; the cases where the
two diverge are often the substantive payoff.
}
\examples{
\donttest{
sim <- simulate_dynamic_dbn(n = 8, time = 12, seed = 1)
fit <- dbn(sim$Z, model = "dynamic", family = "gaussian",
           nscan = 200, burn = 100, verbose = FALSE)
dbn_leverage(fit, H = 3)
}
}
\seealso{
\code{\link[=compute_irf]{compute_irf()}}, \code{\link[=build_shock]{build_shock()}}, \code{\link[=dbn_coupling_rank_probs]{dbn_coupling_rank_probs()}}
}
\author{
Tosin Salau and Shahryar Minhas
}
