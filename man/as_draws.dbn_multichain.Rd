% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multichain.R
\name{as_draws.dbn_multichain}
\alias{as_draws.dbn_multichain}
\title{Convert dbn_multichain to a posterior::draws_array}
\usage{
as_draws.dbn_multichain(x, include_operators = FALSE, ...)
}
\arguments{
\item{x}{A \code{dbn_multichain} object.}

\item{include_operators}{Logical (default \code{FALSE}). If \code{TRUE},
include per-cell \code{A_t}, \code{B_t}, and \code{M} entries (variable names of
the form \code{A[i,j,t]}, \code{B[i,j,t]}, \code{M[i,j]}).}

\item{...}{Unused.}
}
\value{
A \code{posterior::draws_array} object.
}
\description{
Builds an \verb{[iterations, chains, variables]} array of scalar
parameters (sigma^2, sigma^2_obs, tau_A^2, tau_B^2) for downstream
use with the \code{posterior} package: rank-normalised split-Rhat, ESS,
summary tables, and bayesplot.
}
\details{
Operator parameters (\code{A_t}, \code{B_t}, \code{M}) are not included by default
because the \code{posterior} array layout flattens variables into a
character-named axis; including thousands of operator entries
explodes the size. Use \code{include_operators = TRUE} to opt in.

Requires the \code{posterior} package.
}
\author{
Tosin Salau and Shahryar Minhas
}
