% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gof.R
\name{gof.dbn}
\alias{gof.dbn}
\title{Goodness-of-fit by network statistic over time}
\usage{
\method{gof}{dbn}(
  fit,
  stats = c("density", "reciprocity", "transitivity", "mean_in_degree",
    "mean_out_degree"),
  stat_funs = NULL,
  draws = 50L,
  level = 0.95,
  threshold = NULL,
  seed = NULL,
  ...
)
}
\arguments{
\item{fit}{A \code{dbn} fit object (dynamic / static; HMM and low-rank
are not yet supported).}

\item{stats}{Character vector of built-in statistics to include.
Subset of: \code{"density"}, \code{"reciprocity"}, \code{"transitivity"},
\code{"mean_in_degree"}, \code{"mean_out_degree"}. Default: all five.}

\item{stat_funs}{Optional named list of functions taking an
\verb{n_row x n_col} matrix and returning a scalar. If supplied,
overrides \code{stats}.}

\item{draws}{Number of posterior-predictive draws to use (default 50).
For posterior-package convention, \code{nsim} and \code{n_draws} are accepted
aliases via \code{...}.}

\item{level}{Coverage level for the per-time PI (default 0.95).}

\item{threshold}{Edge threshold for stats that need to binarise
continuous data (default \code{NULL}; passed to the underlying
\verb{stat_*} helpers).}

\item{seed}{Optional RNG seed for reproducibility of the PPD draws.}

\item{...}{Additional arguments. \code{nsim} and \code{n_draws} are accepted as
aliases for \code{draws} (matching the \code{posterior} package's convention).}
}
\value{
A data frame with columns \code{stat}, \code{t}, \code{observed},
\code{posterior_mean}, \code{ci_lo}, \code{ci_hi}, \code{in_ci}. Class
\code{c("dbn_gof", "data.frame")}.
}
\description{
For each requested network statistic and each time slice, compares
the observed value to the posterior-predictive distribution and
returns a tidy data frame with observed/posterior_mean/ci_lo/ci_hi/
in_ci columns. The default statistics are network-summary scalars
(density, reciprocity, transitivity, mean in-/out-degree); custom
statistics may be passed via the \code{stat_funs} argument as a named
list of functions.
}
\details{
This is the analog of \code{ergm::gof()} for dynamic DBN fits: rather
than asking "did the model recover the global structural statistics
of the observed network?", it asks "for each time period, does the
observed statistic fall inside the model's posterior-predictive
interval?".
}
\examples{
\donttest{
sim <- simulate_dynamic_dbn(n = 6, time = 8, seed = 1)
fit <- dbn(sim$Z, model = "dynamic", family = "gaussian",
           nscan = 200, burn = 100, verbose = FALSE)
g <- gof(fit, stats = c("density", "reciprocity"), draws = 30)
head(g)
}
}
\seealso{
\code{\link[=posterior_predict_dbn]{posterior_predict_dbn()}}, \code{\link[=stat_density]{stat_density()}},
\code{\link[=stat_reciprocity]{stat_reciprocity()}}, \code{\link[=stat_transitivity]{stat_transitivity()}}
}
\author{
Tosin Salau and Shahryar Minhas
}
