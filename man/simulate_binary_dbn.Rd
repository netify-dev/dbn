% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_binary_dbn}
\alias{simulate_binary_dbn}
\title{Simulate a dynamic binary DBN dataset (convenience wrapper)}
\usage{
simulate_binary_dbn(n = 12, time = 10, keep_relation_dim = TRUE, ...)
}
\arguments{
\item{n}{Number of actors.}

\item{time}{Number of time periods.}

\item{keep_relation_dim}{Logical. If \code{TRUE} (default) the returned
\code{Y} is a 4D \verb{[n, n, 1, time]} array. If \code{FALSE}, the singleton
relation axis is dropped and \code{Y} is a 3D \verb{[n, n, time]} array,
which matches what \code{dim(Y)[3]} users expect to be \code{time} rather
than \code{1}.}

\item{...}{Passed to \code{\link[=simulate_dynamic_dbn]{simulate_dynamic_dbn()}} (e.g. \code{sigma2},
\code{tauA2}, \code{symmetric}, \code{seed}).}
}
\value{
A list with elements \code{Y} (binary array; shape depends on
\code{keep_relation_dim}), \code{Theta} (the underlying latent state),
and \code{truth} (the operator trajectory and cuts).
}
\description{
Generates a binary \verb{[n_row, n_col, 1, time]} array by simulating
continuous latent dynamics via \code{\link[=simulate_dynamic_dbn]{simulate_dynamic_dbn()}} and
thresholding the latent state at zero via the probit link.
}
\examples{
\donttest{
sim <- simulate_binary_dbn(n = 6, time = 10, seed = 1)
table(sim$Y, useNA = "always")
fit <- dbn(sim$Y, model = "dynamic", family = "binary",
           nscan = 200, burn = 100, verbose = FALSE)
}
}
\seealso{
\code{\link[=simulate_dynamic_dbn]{simulate_dynamic_dbn()}}, \code{\link[=dbn]{dbn()}} with \code{family = "binary"}.
}
\author{
Tosin Salau and Shahryar Minhas
}
