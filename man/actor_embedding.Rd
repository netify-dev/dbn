% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dbn_accessors.R
\name{actor_embedding}
\alias{actor_embedding}
\title{Per-actor coupling-profile embedding for a dbn fit}
\usage{
actor_embedding(
  fit,
  what = c("send", "receive"),
  summary = c("mean", "median"),
  fun = NULL
)
}
\arguments{
\item{fit}{A \code{dbn} fit object (dynamic / piecewise / HMM). Static
fits return a single-column matrix with the static operator's row
norm.}

\item{what}{Character: which role to summarise. \code{"send"} (default)
pulls \code{A_t}; \code{"receive"} pulls \code{B_t}. Ignored on symmetric fits
(B = A by construction).}

\item{summary}{Character: \code{"mean"} (default) returns posterior-mean
row norms; \code{"median"} returns posterior-median row norms (more
robust under heavy-tailed posteriors).}

\item{fun}{Optional user-supplied scalar function of an
\verb{n_row x n_col} matrix slice, e.g. \code{function(M) max(abs(M))}. When
provided, overrides the default row-norm reduction. Should return
a numeric vector of length \code{n_row}.}
}
\value{
Numeric matrix. For static fits a \verb{n_row x 1} matrix (the
row norm of the single operator). For dynamic / piecewise fits a
\verb{n_row x (Tt - 1)} matrix: the \code{t = 1} column is dropped because
\code{A_0} is pinned at the identity anchor (every row norm is exactly
1, which isn't an estimated parameter). Under auto-time-thinning,
columns are named from \code{dimnames(fit$Y)[[4]][fit$time_kept[-1]]}
rather than \verb{[-1]} of the full time axis, so the labels match the
stored slices.
}
\description{
Extracts an actor-by-time matrix of scalar coupling scores summarising
how each row-actor's sender (or receiver) role evolves over the fit's
time horizon. The default scalar is the per-actor row norm of the
posterior-mean transition operator: \verb{||A_t[i, ]||} for \code{what = "send"}, \verb{||B_t[i, ]||} for \code{what = "receive"}. These are the
coupling-strength scores that paper with \code{dbn_coupling_rank_probs()}
and \code{compute_irf()}.
}
\details{
The intended use is "give me a tidy matrix I can hand to xgboost,
write to CSV for Python, or cluster with kmeans" for graph-mining
and downstream-ML pipelines.
}
\examples{
\donttest{
sim <- simulate_dynamic_dbn(n = 6, time = 8, seed = 1)
fit <- dbn(sim$Z, model = "dynamic", family = "gaussian",
           nscan = 200, burn = 100, verbose = FALSE)
emb <- actor_embedding(fit, what = "send")
dim(emb)  # n_row x (Tt - 1) -- t=1 anchor column is dropped
}
}
\seealso{
\code{\link[=coef.dbn]{coef.dbn()}}, \code{\link[=dbn_coupling_rank_probs]{dbn_coupling_rank_probs()}}, \code{\link[=compute_irf]{compute_irf()}}
}
\author{
Tosin Salau and Shahryar Minhas
}
